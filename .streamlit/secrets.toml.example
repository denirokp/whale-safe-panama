# Copy to secrets.toml (git-ignored) for local live data, or paste the same
# line into Streamlit Community Cloud → app → Settings → Secrets when deploying.
# Free key: https://aisstream.io (sign in with GitHub).
AISSTREAM_API_KEY = ""
